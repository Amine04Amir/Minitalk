#include "minitalk.h"

int ft_atoi(int pid)
{
    
}

int main(int ac, char **av)
{
    if (ac < 0)
        return NULL;
    int pid = ac[1];
    ft_atoi(pid);
    while ()
    {
        kill(pid, SIGUSR1);    

    }

}